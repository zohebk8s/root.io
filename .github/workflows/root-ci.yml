name: Root.io Remediation Only

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  remediate:
    runs-on: ubuntu-latest
    environment: Root
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Root.io Remediation
        uses: rootio-avr/rootio-remediation-action@latest
        with:
          org_id: ${{ vars.ROOTIO_ORG_ID }}
          api_token: ${{ secrets.ROOTIO_API_TOKEN }}
          registry_credentials_id: ${{ vars.ROOTIO_REGISTRY_CREDENTIALS_ID }}
          image_reference: zoheb3454/keycloak:26.3.3-debian-12-r0
